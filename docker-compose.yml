services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${POC_HTTP_PORT:-9081}:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1/"]
      interval: 15s
      timeout: 5s
      retries: 3
